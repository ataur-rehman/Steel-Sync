// Add this button to any component where you want quick access

const BalanceDiagnosticButton = () => {
  const navigate = useNavigate();
  
  return (
    <button 
      onClick={() => navigate('/debug/balance')}
      className="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded"
      title="Balance Diagnostic Tool"
    >
      🔍 Debug Balance
    </button>
  );
};
